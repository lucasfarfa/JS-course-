'use strict';

var num = 1;
var num2 = 5;

// Suma
var resultado = num + num2;
console.log(`${num} + ${num2} es ${resultado}`);
// Resta
resultado = num - num2;
console.log(`${num} - ${num2} es ${resultado}`);
// Divisionn
resultado = num / num2;
console.log(`${num} / ${num2} es ${resultado}`);
// Multiplicaicion
resultado = num*num2;
console.log(`${num} x ${num2} es ${resultado}`);