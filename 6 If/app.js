'use strict';

var edad = 19;

if(edad >= 18) {
    console.log('Eres mayor de edad.');
} else {
    console.log('Eres menor de edad.');
}

if (edad >= 18 && edad <= 30) {
    console.log('Estas entre 18 y 30.');
} else {
    console.log('Muy chico o muy grande xd.');
}

// == compara conteenido
// === compara contenido y tipo de dato
// > Mayor que >= Mayor igual que ( > = )
// < Menor que <= Menor igual que( < = )
// != diferente contenido (! = )
// !== diferente contenido y dato (! = = )

// AND && -> Si ambas son verdaderas da true
// OR || -> Si una es verdadera da true
// NOT ! -> Si una es falsa de false